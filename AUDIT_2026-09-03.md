# Audit 2026-09-03 — Findings & Fix Plan

The Architect audit (14 dimensions, 14 parallel investigators). **Overall: A-** ·
0 CRITICAL · 2 HIGH (regression-lock gaps) · ~6 MEDIUM · ~6 LOW.
Scorecard: S1 A+ · S2 A- · S3 A+ · S4 A- · S5 A+ · S6 A · S7 A+ · A1 B+ · A2 B+ ·
A3 A · A4 A- · A5 A · A6 B+ · A7 A-.

This doc is the WORKING TRACKER: every finding, its fix plan, and live status.
Statuses: ☐ todo · ▶ in progress · ✅ fixed · ⏸ blocked · 📋 proposed (needs Kevin) ·
🔍 verified-non-issue.

---

## HIGH — regression locks on new money-adjacent code

### H1 ☐ `admin_quarantine_upload` admin gate untested (A6)
- **Evidence:** migration 449 gate is correct (verified by S2+S3) but no test locks it —
  the exact pattern that let refund_sparkles' guard silently regress historically.
- **Plan:** new `__tests__/db/adminQuarantineUpload.dbspec.ts` modeled on
  refundSparkles.dbspec.ts: extract mig-449 DDL via the `_support/pg.ts` harness; matrix:
  admin quarantines any upload ✓ / non-admin → 'Not authorized' (other's AND own post) /
  quarantine flips is_public=false + stamps quarantined_at + reason default. dbspecs are
  CI-only: validate by push + `gh run watch` the db-tests job.
- **Done when:** dbspec lands + CI db-tests green.

### H2 ☐ `reconcile-subscription-sparkles.js` untested (A6)
- **Evidence:** cycle-start math, yearly/monthly period inference, indeterminate-skip,
  idempotency-reason format — all unlocked.
- **Plan:** extract the DECISION LOGIC into a pure module
  `scripts/lib/subscriptionReconcile.js` (`decideReconcile({periodStamp, lastGrant,
  monthlyBundle, expiresAt, now}) → {action: grant|skip_covered|skip_yearly|
  skip_indeterminate, cycleKey}`); script becomes a thin I/O shell. New fast-jest
  `__tests__/lib/subscriptionReconcile.test.ts`: cycle boundaries (incl. month-length
  edges), stamp-first inference, 12x-amount fallback inference, indeterminate never
  grants, covered-cycle skips, reason-key stability (idempotency contract).
- **Done when:** pure module + tests green + dry-run output identical pre/post refactor.

---

## MEDIUM

### M1 ✅ `types/database.ts` stale since mig 448 (A2) — regenerated from prod schema
  (4,613 lines, all 449-452 columns + RPCs present); 3 `as never` cast sites removed
  (useDeletePost ×2, settings list_my_upload_paths); tsc clean, 1738 tests green.
- **Evidence:** missing uploads.quarantined_at/quarantine_reason/seed_source,
  users.*_subscription_period, RPC admin_quarantine_upload → the `as never` casts in
  hooks/useDeletePost.ts.
- **Plan:** `supabase gen types typescript --project-id jimftynwrinwenonjrlj` →
  types/database.ts (regen is the sanctioned path; hand-editing banned). Then remove the
  now-unneeded casts (useDeletePost.ts admin_quarantine_upload + admin_delete_upload if
  typed; settings list_my_upload_paths). prettier + tsc + jest.
- **Done when:** regenerated, casts removed, tsc/jest green.

### M2 ☐(scrub) / ⏸(rotate) Anthropic key in `.claude/settings.local.json` (S6)
- **Evidence:** two permission entries embed a real `sk-ant-api03-…` key. Untracked,
  never shipped — but plaintext on disk and echoed into tool logs.
- **Plan:** scrub the two entries from the file (safe local edit). **Rotation is
  Kevin's:** console.anthropic.com → rotate the key → update .env.local + Supabase edge
  secrets + GH Actions secret ANTHROPIC_API_KEY.
- **Done when:** entries scrubbed. ⏸ KEVIN: rotate the key when convenient.

### M3 ☐ Render-budget constants duplicated (A1 — mine, from the budget-split fix)
- **Evidence:** RENDER_DEADLINE_MS / SOLO_FALLBACK_RESERVE_MS / DUAL_RECOVER_MS /
  SOLO_RECOVER_MS identical in nightly-dreams + generate-dream.
- **Plan:** extract to `supabase/functions/_shared/renderBudgets.ts` (as const), import
  in both; deno typecheck; deploy BOTH fns.
- **Done when:** shared module, both fns import it, deployed.

### M4 📋 Subscription tier config 4-way scatter (A1/S7) — PROPOSED, needs Kevin
- **Evidence:** engine_config is the true SoT (webhook + reconcile read it live); but
  constants/proPlan.ts + basicPlan.ts hardcode display copies (75/20/900/240, trial 14) —
  a dashboard bundle change leaves the paywall showing stale numbers until an app build.
- **Proposed plan (NOT built):** client fetches the 4 display values from engine_config
  at startup (existing config-fetch path), caches in the auth/config store, constants
  become fallbacks only; plus a daily audit query (webhook-failure detector: users where
  expires_at > now() but flag=false). ~1-2h. **Say go and I'll build it.**

### M5 ⏸ Trial-farming window (S4) — BLOCKED on Kevin / needs approval
- DeviceCheck built + deployed but INERT until the `.p8` key lands (Kevin).
- 📋 Interim Phase 0 (needs approval): disposable-email blocklist + alias normalization
  at the first-dream gate. Say go to build.

### M6 ☐ `ANTHROPIC_KEY!` ×3 + module-level `REPLICATE_TOKEN!` (A2)
- **Plan:** explicit null-guards with loud errors instead of `!` (generate-dream ×2,
  nightly-dreams ×1); describe-photo's module-scope `!` → lazy getter so a missing env
  fails per-request with a clear message, not at boot. Deploy touched fns.
- **Done when:** guards added + touched fns deployed.

---

## LOW

### L1 ☐ `basic_subscription_will_renew` freeze (S2 — likely false positive, sealed anyway)
- **Plan:** FIRST verify via column_privileges whether authenticated holds UPDATE on the
  column (settles the false-positive question). Regardless: migration 453 adds it to
  freeze_user_columns_on_update beside its siblings (belt-and-suspenders). Kevin applies.

### L2 ☐ CLAUDE.md edge-fn list stale (A7)
- **Plan:** "Active (15)" → 16, add audit-cast-photos.

### L3 ☐ `IDENTITY_DEGRADE_FLOOR` hardcoded (A7)
- **Plan:** engine_config.identity_degrade_floor (mig 454, default 0.25); callers read
  config and pass as new opt `identityDegradeFloor` to genderSafeDualSwap (default keeps
  0.25 — jest unaffected); deploy nightly-dreams + generate-dream.
- **Done when:** mig 454 applied (Kevin) + opt threaded + both fns deployed.

### L4 ☐ Naming honesty: rename quarantine path (A7)
- **Plan:** handleAdminDelete→handleAdminQuarantine, onAdminDelete(Immediate)→onAdminQuarantine…,
  adminDeleteCircle→adminQuarantineCircle, useAdminShowDeleteButton→
  useAdminShowQuarantineButton (AsyncStorage KEY unchanged so Kevin's toggle survives).
  `useAdminDeleteComment` untouched — comments really are deleted.

### L5 ☐(verify) `_tmp-*` scripts (A7)
- **Plan:** check tracked vs untracked. If untracked → another agent's WIP, not mine to
  touch. If tracked → archive.

### L6 ☐ Dead tsconfig exclude entry `dualGenderRouting.test.ts`
- **Plan:** verify the file truly doesn't exist, then remove the exclude entry.

### L7 📋 Reconcile N+1 (A4): fine at current scale; refactor to bulk-fetch when paying
  subs approach ~100. Documented threshold; revisit trigger = subscriber count.

### L8 ☐(verify) share_count/save_count maintenance (A3 monitor-flag)
- **Plan:** verify both counters are maintained only via SECURITY DEFINER paths (find the
  trigger/RPC migrations); document the evidence or add the missing trigger.

---

## Deferred / standing (pre-existing, documented)
- DeviceCheck activation (M5) — Kevin's `.p8`.
- Tier-config client consolidation (M4) — proposed.
- Disposable-email blocklist (M5) — proposed.
- nightly-dreams monolith decomposition — explicitly deferred (defensible; revisit when a
  NEW dream path type is added).
- Monitor config-lock pattern rollout (ai-failure/db-health thresholds) — follow the
  botCadence.test.ts pattern opportunistically.

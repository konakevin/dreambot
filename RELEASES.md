# RELEASES.md — the DreamBot release ledger

One row per **shipped** App Store build (the ones submitted for review, not every
TestFlight iteration). This is the human-readable changelog + the map from a git
tag to its EAS build number and commit. Update it as the LAST step of every
release (`scripts/release.sh` prints the reminder).

Tag convention: annotated `v<marketing-version>` on the exact commit the build was
cut from. If one marketing version ships more than one build (a resubmission after
rejection), the extra builds get `v<version>-build<N>` and `v<version>` stays on
the build that was ultimately **approved**. See `RELEASE.md` for the full runbook.

| Tag | Version | Build | Commit | Shipped | ASC status | Notes |
|-----|---------|-------|--------|---------|-----------|-------|
| `v1.0.13` | 1.0.13 | 43 | `a84495d3` | 2026-08-07 | Uploaded (processing) | LOCAL build. Hotfix: New Post **caption character-repeat** bug — the composer's controlled `<TextInput value>` also had text children (inline colored @mentions), which double-applies content on the New Architecture (Fabric), so every keystroke re-appended the whole value. Removed the children (autocomplete unaffected; mentions still color in the posted caption). Added a static **ESLint failsafe** `rn/no-textinput-value-with-children` (eslint-rules/, locked by a RuleTester jest test) so the anti-pattern can't ship again — jest/RTL can't reproduce this native-render class. Also Meta **SKAdNetwork** IDs (`43eb0879`) for App-Install ad measurement. Tag moved to the fixes commit `a84495d3` (the version-bump commit was `6bd9b366`). |
| `v1.0.12` | 1.0.12 | 41 | `4a29dcd4` | 2026-08-06 | Released (live, forced min) | LOCAL build. New: @-mention **autocomplete** in comments + captions (follow-scoped, caret-aware) — caption tags are now discoverable/typeable, and the `post_mention` notification path (inbox + push) was verified firing end-to-end. Nightly dreams now fire during each user's **LOCAL night** (timezone-aware, hourly cron at-or-after the target local hour, robust to skipped cron hours). Feed/refresh stability; clearer HD-save copy. **DeviceCheck trial-farming gate** shipped + activated (Anchor A — a device that already used the free trial can't re-farm it with a new email; fail-open; `PrivacyInfo.xcprivacy` Device ID → App Functionality, not linked, not tracking; ASC label matched; `TRIAL_GATE_CHECKLIST.md`). Audit-hardening: `signOut` clears all entitlement fields, `usePostLikes` bounded (no 1000-row truncation), new-scene tier pricing single-sourced across enqueue/generate/client + **parity-locked** (charge==render), silent-catch logging on the `dream_jobs` done-flip + style-summary, rate-limit retry-before-fail-open, `view_count` now trigger-maintained (mig 432). Tag on release commit `4a29dcd4`. (NOTE: 1.0.11 shipped — gates were at 1.0.11 — but never got a ledger row.) |
| `v1.0.10` | 1.0.10 | 39 | `132f5d3a` | 2026-07-23 | Released (live, forced min) | LOCAL build. New: Grok (xAI) image model in Create + DreamSmart now covers every art style (flux-1.1-pro/ultra across all 20 styles). Fresher feed — restored impression discounting (mig 388), jitter clamp (389), age-aware seen-penalty waiver (390), candidate windowing (391), TikTok-style cold open (random launch seed, feed not persisted). Nightly cast roll rebalanced to 40% dual / 30% self / 30% +1. Fixed feed getting stuck after idle (InteractionManager pager-hydration deadlock + unit-tested latch). Home re-tap spinner centered over feed. Clearer no-photo Create placeholder. Backend-only follow-on on the tag tip: canvas per-model flux_fragment override (Nano Banana Pro easel fix, not in binary). Tag on tip `132f5d3a`; version-bump commit `28c77a77`. |
| `v1.0.9` | 1.0.9 | 37 | `52636f9163` | 2026-07-21 | Released (live, forced min) | LOCAL build (cloud quota exhausted). Supersedes builds 33 (`161aed2436`) and 32 (34-36 = aborted attempts). New over 33: universal-button-lag fix (feed-cache pruning + skip-scan sweeps), quiet Home reshuffle (tab-icon spinner + one-frame remount, no black cover), image-perf (prefetch staleTime + hero priority), share-sheet slide-up + full backdrop mask, TikTok-style tab squish (0.92) + filled active icons, Dream/Post first-tap fixes, Create-header keyboard dismiss, deleted-post disappearance fix. Carries from 32/33: caption @mention notifications (`post_mention`) + inbox copy, Create-screen freeze fix, live dream-grid updates, instant heart, Edit description + "Share again", likes-sheet swipe-dismiss, local-build infra (fastlane, FB env secret→sensitive + out of eas.json). |
| `v1.0.8` | 1.0.8 | 24 | `2098835723` | 2026-07-19 | Released (live, forced min) | Comment sheet redesign (swipe-to-dismiss, single-tap post, oldest-first ordering); repost your own dreams + view anyone's public reposts album; privacy controls (hide reposts on private dreams, account-level Allow Downloads); free trial now starts at onboarding completion, not signup (+ backfill); faster feed cold-start + lighter grid thumbnails; Real Face styles; inline Change Password + branded password-reset email; onboarding/settings readability polish + welcome-gift cleanup; smoother multi-select grid scroll + delete-confirm wording. Also bot dark-launch shadow paths (admin-only, no user surface) and server-side analytics/audit hardening. |
| `v1.0.7` | 1.0.7 | 23 | `c4e14548d9` | 2026-07-13 | Submitted | DreamSmart style-aware model curation + on/off toggle, medium-first Create layout; "Dream this again" per album slide; Dream Cast roster up to 5 loved ones; welcome-gift redesign ("2 weeks free", two-pillar nightly + sparkles); Dynamic Type / Larger-Text clip fixes across titles, pills, avatars; faster album open (warm prefetch + fade-in); dead-letter refund for hard-killed jobs; image-transform quota fix; trademark-free labels (LEGO→brick); pull-refresh + live-update polish. |
| `v1.0.6` | 1.0.6 | 21 | `c3d0631a5d` | 2026-07-11 | Submitted | Gallery albums (dissolve vs delete, live-refresh) + consolidated album-navigation capsule + grid actions (save-all, per-slide HD, visibility toggles); album live-update fix, frozen-inbox-after-delete fix, reset-profile 1000+ storage cleanup, distinct new-post icon; render-path reliability hardening (poll-failover, jitter, vision retry). Build 22 (`fe1a7308a9`) was a same-version follow-up iteration. |
| `v1.0.5` | 1.0.5 | 18 | `3119a43d13` | 2026-07-11 | Uploaded (processing) | Build 18 (builds 16 + 17 submissions cancelled pre-release). Adds over 17: "Medium" → "Style" UI rename (all surfaces), liked posts can never show a blank like count, unified Surprise Me face/art split. Carries everything from 16/17: fatal Create-screen crash fix, onboarding-lockout fix (transient read can't route an established user to onboarding), failed-dream retry reopens in Create prefilled, multi-image album posts + unified posting flow + bulk album edit, per-dream notifications + accurate badge, follow-request accept/deny on profile, Search/Explore exact-tap fix, startup-logo size-pop fix, audit hardening. |
| `v1.0.4` | 1.0.4 | 15 | `2dfa076a78` | 2026-07-10 | Uploaded (processing) | Dream queue enabled in store builds (background-safe renders, caps, retries), provider failover on AI outages + failure observability, comment-sheet keyboard/gesture overhaul + swipe-away keyboard on Create/Edit Profile, postcard nightly captions, goofy scenario categories, action-grounded briefs, BrandSpinner polish. |
| `v1.0.3` | 1.0.3 | 14 | `a314947d39` | 2026-07-09 | Submitted | Home Screen widget, New Scene photo mode (Standard/Ultra tiers), face-swap quality program (restore + identity gate on every swap), action pose/scenario expansion + no-repeat shuffle, feed refresh overhaul (quiet re-tap, brand spinner suite, real reshuffle), production analytics/crash-reporting fix. |
| `v1.0.2` | 1.0.2 | 10 | `987c68c28f` | 2026-07-07 | Submitted | Gift Sparkles, pinned posts, tappable hashtags, Dream this again, dream-notification aggregation + Dreams-tab auto-ack, comment profile taps + @mentions, share-sheet + create-screen polish, cast-photo / pull-to-refresh / album fixes. |
| `v1.0.1` | 1.0.1 | 9 | `805519184c` | 2026-07-05 | Submitted | Followers-list crash fix, block-aware profile counts, Message button removed. |
| `v1.0.0` | 1.0.0 | 8 | `9feb0c4200` | 2026-07-03 | Approved (first release) | First App Store submission. |

<!--
TestFlight / non-shipped production builds (from `eas build:list`, for reference —
NOT tagged): 1.0.0 b7 (canceled), b6, b5, b4, b3, b2 (errored). Only builds that
were submitted for App Store review get a row + a tag above.
-->

## How to add a row

After a build finishes and you submit it in ASC:

1. `eas build:list --platform ios --limit 1` → note the build number + commit.
2. Add a row at the TOP of the table (newest first) with the tag, version, build,
   short commit, date, ASC status (`Submitted` → later `Approved` / `Rejected`),
   and a one-line "what changed".
3. Commit `RELEASES.md` (the release commit or a follow-up — either is fine).

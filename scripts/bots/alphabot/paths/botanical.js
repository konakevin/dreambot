/**
 * DreamBot botanical path (2026-06-28) — OUTDOOR storybook/nostalgic lush plant-life
 * scenes as the hero (NOT a single flower bloom; BloomBot owns flowers; never an
 * indoor atrium/greenhouse/conservatory). Biome-diverse hero pool + biome-neutral
 * setting + ~60%-gated storybook scene_element (paths/bridges/gates/ruins). Own
 * botanical fine-art medium; excluded from the look-rotation; skip chaos + polish.
 */
module.exports = {
  archetype: 'DREAMBOT_BOTANICAL',
  pools: {
    botanical: 'BOTANICAL_HERO',
    setting: 'BOTANICAL_SETTING',
    light_season: 'BOTANICAL_LIGHT_SEASON',
    framing: 'BOTANICAL_FRAMING',
    scene_element: 'BOTANICAL_ELEMENT', // ~60% gate
  },
};

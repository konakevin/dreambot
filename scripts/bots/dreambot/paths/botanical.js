/**
 * DreamBot botanical path (2026-06-28) — lush foliage / trees / fungi / gardens as
 * the hero (NOT a single flower bloom; BloomBot owns flowers). Own botanical
 * fine-art medium; excluded from the look-rotation; skip chaos + polish.
 */
module.exports = {
  archetype: 'DREAMBOT_BOTANICAL',
  pools: {
    botanical: 'BOTANICAL_HERO',
    setting: 'BOTANICAL_SETTING',
    light_season: 'BOTANICAL_LIGHT_SEASON',
    framing: 'BOTANICAL_FRAMING',
  },
};
